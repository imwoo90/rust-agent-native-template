---
description: Agent-Native Rust architectural standards, character budgets, and compiler quality rules
globs: src/**/*.rs, tests/**/*.rs, Cargo.toml
alwaysApply: true
---

# Agent-Native Rust Guidelines

This codebase strictly enforces the **Agent-Native** standards defined in `AGENTS.md`.

- **Single Source of Truth (SSOT)**: Always consult and strictly adhere to `AGENTS.md` before generating, modifying, or reviewing code. All character limits, living wiki headers, and lint rules are maintained in `AGENTS.md`.
- **Required Verification**: Execute the full verification suite defined in `AGENTS.md` before completing any task.
